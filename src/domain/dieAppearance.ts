export interface DieAppearance {
  readonly background: string;
  readonly pipColor: string;
}

export const DEFAULT_DIE_APPEARANCE: DieAppearance = {
  background: '#ffffff',
  pipColor: '#111111',
};
